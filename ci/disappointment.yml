---
platform: linux

image_resource:
  type: registry-image
  source: {repository: rafakato/alpine-graphicsmagick}

inputs:
- name: web-development

outputs:
- name: jimmer

run:
  path: web-development/ci/jimmer
